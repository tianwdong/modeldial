# Case Stream Explorer v17

This is the production-owned frontend evaluation contract. A candidate receives one self-contained HTML task. Playwright awards 85 deterministic points and captures anonymous state evidence; an independent `gpt-5.6-sol / max` visual judge awards the remaining 15 points.

The coding score and frontend score remain separate axes. This package does not define a weighted overall ranking.
