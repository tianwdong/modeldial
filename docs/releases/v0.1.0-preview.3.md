# ModelDial v0.1.0-preview.3

> **UNSIGNED / UNNOTARIZED PREVIEW**
>
> 这是需要手动安装和放行的阶段性预览包，没有 Apple Developer ID 签名，也未经过 Apple notarization。它不是正式 `v0.1.0`，不能保证首次双击直接打开。

本文件是拟发布 GitHub prerelease `v0.1.0-preview.3` 的版本库正文来源。截至 2026-08-09，该 tag、Release 和公开下载尚未创建。

## 本次修复

- 优化顶部胶囊展开／收起的主线程工作量、内容挂载节奏和命中区，降低 Hover 与点击卡顿；收起时“箭头＋标题＋左侧空白”保持为一个完整点击区域。
- 首次打开可直接浏览并自动刷新官方 Radar；本地 Provider 和本机评测继续保持可选。
- 双语 README 更新产品动图、DMG 安装提示和 [modeldial.com](https://modeldial.com) 入口。
- unsigned preview 继续禁用未发布的 Sparkle 更新通道；`SUFeedURL` 与 `SUPublicEDKey` 均为空，升级仍需手动下载新版本。
- marketing version 为 `0.1.0`，build number 为 `102`；App 记录精确源码提交 `16e0dd2cafa82ef1b77b719edc0a3db90e5bf68f`。

## 平台

- macOS 13 或更高版本
- Apple Silicon（arm64）
- Intel Mac 不支持

## 候选资产

- `modeldial-0.1.0-preview.3-macos-arm64.dmg`：`17,631,107` bytes；SHA-256 `b0489c39b5e503b634c44f52f03e6d07bd60908f96af2b993cf161a51caa2082`
- `modeldial-0.1.0-preview.3-build-102-macos-arm64.zip`：`15,405,346` bytes；SHA-256 `e9ce7e5159125725f87ea2becb30bac957e2daffe87e0ddc849d0e9ab2d80f26`
- `modeldial-0.1.0-preview.3-sbom.spdx.json`：`200,931` bytes；SHA-256 `d66d74078108b6c745dc542d0e130d6b41f8a60930a27f74f5a5d7a14ad40e4e`
- `SHA256SUMS`

公开后，请从同一 Release 获取上述资产和 `SHA256SUMS`，在下载目录运行：

```bash
shasum -a 256 -c SHA256SUMS
```

校验失败时不要安装；请重新下载并确认 Release 标签为 `v0.1.0-preview.3`。

## 首次安装

1. 打开 DMG，把 `modeldial.app` 拖到 `Applications`，推出 DMG 后从 `Applications` 启动。
2. 如果 macOS 阻止首次打开，关闭提示，进入“系统设置 → 隐私与安全性”。
3. 在安全性区域找到 ModelDial 被阻止的提示，点击“仍要打开（Open Anyway）”并确认，再从 `Applications` 启动。
4. 如果没有显示“仍要打开”，重新尝试打开一次 App，再回到“隐私与安全性”查看。不同 macOS 小版本的提示文字可能略有差异。

不需要关闭 Gatekeeper。不要执行 `xattr -dr com.apple.quarantine`、`spctl --master-disable` 或其他绕过系统安全检查的命令；如果来源无法确认，请停止安装并重新核对资产和 SHA-256。

## 本地候选已完成验证

- 打包前完成构建合同定向回归 `28/28`、全量 Python `1421/1421`（`ResourceWarning` 按错误处理）和 `git diff --check`；release-only packaging 从干净提交 `16e0dd2cafa82ef1b77b719edc0a3db90e5bf68f` fresh build。
- `SHA256SUMS` 三项自校验、DMG 容器校验及只读挂载、ZIP 完整解包均通过；DMG 同时包含 `/Applications` 拖放入口和中英文手动放行说明。
- DMG 与 ZIP 内 App 均回读为 `0.1.0（Build 102）`、同一完整源码提交、`https://reference.modeldial.com/reference-snapshots`、空 Sparkle feed／公钥和 thin arm64。
- 两份 App 均通过深层严格 ad-hoc 签名、60 个 Mach-O／65 个架构记录的 macOS 13 兼容门禁，以及同一 SPDX SBOM 的 bundle／requirements 复验；DMG 与 ZIP 主程序 SHA-256 一致。
- ZIP 内冻结后端在隔离 HOME 和全新数据目录从官方端点刷新成功，delivery 为 `http/refreshed`，得到 `snapshot-2026-08-09T00-00-00Z` 的 15 条结果；provenance 标记为公开官方第一方快照，本地缓存再次校验通过。

## 仍待发布后验证

- 创建 annotated tag 和 GitHub prerelease、上传四项精确资产，并从无认证公开 URL 重新下载复验。
- 当前验收机器的 Gatekeeper assessments 已关闭，尚未在 Gatekeeper 开启的独立 macOS 13+ Apple Silicon 机器上完成“仍要打开”和真实安装验收。

## 已知限制

- 没有 Developer ID、secure timestamp、Apple notarization 或 stapling；当前验证不等于正式发行验收。
- 预览版不进入正式 Sparkle `appcast.xml`，不提供自动更新或 Homebrew Cask。
- 不支持 Intel Mac，Windows 客户端仍是后续独立里程碑。
