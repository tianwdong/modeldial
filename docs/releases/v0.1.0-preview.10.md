# ModelDial v0.1.0-preview.10

> **UNSIGNED / UNNOTARIZED PREVIEW**
>
> 该预览包没有 Developer ID 签名，也未经过 Apple notarization。若 macOS 阻止首次打开，请使用“系统设置 → 隐私与安全性 → 仍要打开”，不要关闭或绕过 Gatekeeper。

这是当前 Apple Silicon 预览版，对应 `0.1.0（Build 109）`，精确源码提交为 `d60488b4d2d22ca35f44f552521ccb5354fc5640`。

## 本版变化

- 修复第一方雷达快照缓存的原子读取，避免中断刷新留下混合或过期状态。
- 启动时先恢复已保存界面，并对恢复／观察维护失败执行最多三次有界重试。
- 手动刷新与启动刷新不再互相吞掉，最新官方雷达数据可正确写入并复用缓存。

## 下载与安装

Homebrew（个人 Tap）：

```bash
brew install --cask tianwdong/tap/modeldial
```

该 Cask 下载本 Release 的同一 DMG 并固定 SHA-256。由于当前 App 未签名、未公证，Cask 会仅对安装目标中的 `modeldial.app`（默认 `/Applications/modeldial.app`）递归移除 `com.apple.quarantine`，不使用 `sudo`，不关闭 Gatekeeper，也不修改系统级安全设置。该行为属于签名前的临时预览策略，可在 [`tianwdong/homebrew-tap`](https://github.com/tianwdong/homebrew-tap) 审查。

DMG：

1. 下载并打开 [`modeldial-0.1.0-preview.10-macos-arm64.dmg`](https://github.com/tianwdong/modeldial/releases/download/v0.1.0-preview.10/modeldial-0.1.0-preview.10-macos-arm64.dmg)。
2. 把 `modeldial.app` 拖到 `Applications`，推出 DMG 后从 `Applications` 启动。
3. `preview.7` 及更高版本也可以进入“设置 → 软件更新”升级；`preview.6` 及更早版本仍需先手动安装新版一次。

## 资产

- `modeldial-0.1.0-preview.10-macos-arm64.dmg`：`17,648,316` bytes；SHA-256 `b03fcadc9e89104c162c18bbba836e2603de861db93db3e1cb159e6aaafd1372`
- `modeldial-0.1.0-preview.10-build-109-macos-arm64.zip`：`15,432,016` bytes；SHA-256 `82e302a8a2a0f90b55baa0f2593df0a5fc752807315a714eb8f7e5454b95a1ff`
- `modeldial-0.1.0-preview.10-sbom.spdx.json`：`200,935` bytes；SHA-256 `5c31876ded0da10dc387d114720d2d4359518704e742b04c47c98070a0af5bd7`
- `SHA256SUMS`

可在下载目录运行：

```bash
shasum -a 256 -c SHA256SUMS
```

## 已完成验证

- 干净源码提交完成 `1,437/1,437` Python／Swift 合同回归；fresh build 后补跑两项依赖构建产物的合同测试。
- 三项 SHA-256、DMG、ZIP、SPDX SBOM、thin arm64、60 个 Mach-O／65 个架构记录的 macOS 13 兼容门禁和深层 ad-hoc 签名通过。
- App 与 ZIP 均回读 Build 109、精确源码提交、官方 Radar 地址、preview appcast、公钥、签名 feed 和解压前验签配置。
- 冻结后端在隔离数据目录完成启动快照、官方 HTTP 刷新和缓存复读；拉取到 `snapshot-2026-08-09T06-00-00Z`。
- GitHub 与 R2 的四项资产均已通过公开 URL 回下载，大小和 SHA-256 与本地候选精确一致；在线 appcast 与 ZIP enclosure 的 Ed25519 签名均通过。
- appcast enclosure 使用同一不可变 R2 对象的 `?v=109` URL，避开发布前存在性检查留下的临时 `404` 边缘负缓存；对象内容和签名没有改变。
- `/Applications` 中的 Build 108 已通过“设置 → 软件更新”发现、下载、验签、安装并重启到 Build 109；再次检查显示“当前已是最新版本”。升级前后的 81 个持久文件无新增或缺失，仅 4 个实时使用观察文件按预期更新。

## 已知限制

- 仅支持 macOS 13+ Apple Silicon；Intel Mac 和 Windows 客户端当前不支持。
- 没有 Developer ID、secure timestamp、Apple notarization 或 stapling；本机应用内升级不替代 Gatekeeper 干净机验收。
