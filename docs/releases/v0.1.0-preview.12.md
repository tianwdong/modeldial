# ModelDial v0.1.0-preview.12

> **UNSIGNED / UNNOTARIZED PREVIEW**
>
> 该预览包没有 Developer ID 签名，也未经过 Apple notarization。若 macOS 阻止首次打开，请使用“系统设置 → 隐私与安全性 → 仍要打开”，不要关闭或绕过 Gatekeeper。

这是面向 macOS 13+ Apple Silicon 的修复预览版。本次发布对应 `0.1.0（Build 111）`，精确源码提交为 `5082523eb69f286d5e6397416f44e10adc9eaefb`。

## 本版变化

- 修复 `preview.11` 在正常受保护 Mac 上因 ad-hoc bundle 错误启用 hardened runtime，导致冻结 Python 后端和嵌套组件被 Library Validation 拒绝加载的问题。
- 新增逐个 Mach-O 的签名策略门禁：unsigned preview 必须全部为 ad-hoc、无 hardened runtime、无证书 Authority 和 Team ID；Developer ID 构建路径继续保留 hardened runtime 与 secure timestamp。
- 本地组件启动失败时，Radar、扫描设置和模型接入改为明确的加载／不可用状态，不再展示虚假计数、空选项或可执行操作；原始 dyld／PyInstaller 信息只在用户主动复制诊断时提供。
- Native Bridge 并行排空 stdout／stderr，并为普通操作和连接探测增加有界超时与终止流程，避免大错误输出或冻结子进程卡住界面。
- 官方 Radar 下载在单次瞬时 `unavailable` 时重试一次完整 feed；HTTPS、同源、大小、哈希和 payload 校验失败仍立即停止。

## 下载与安装

不要继续安装存在后端启动问题的 `preview.11`。当前 `preview.12` 已公开并完成分发回读，可使用以下两种入口：

```bash
brew install --cask tianwdong/tap/modeldial
```

或从本 Release 下载 `modeldial-0.1.0-preview.12-macos-arm64.dmg`，拖入 `Applications` 后启动。由于本版仍未签名、未公证，首次打开可能需要在“系统设置 → 隐私与安全性”中选择“仍要打开”。

GitHub Release 与 R2 镜像包含以下三个发行资产，字节数和 SHA-256 已分别从两个公开入口回读核对：

- `modeldial-0.1.0-preview.12-macos-arm64.dmg`：`17,769,588` bytes；SHA-256 `aee58b52f33b94fc1da3c296cdb345ff59fe76a8e3343b4cd3f3aadb516f8b22`
- `modeldial-0.1.0-preview.12-build-111-macos-arm64.zip`：`15,532,390` bytes；SHA-256 `c43c640f229434c3923c305b3a9bbad41d3e2b3431510ba7387cf003fc98ce64`
- `modeldial-0.1.0-preview.12-sbom.spdx.json`：`200,935` bytes；SHA-256 `4db0f59d7c4263d202a014ab23dac1e5df67d927b2788c99952f2b2637e10158`

校验清单 `SHA256SUMS` 为 336 bytes，文件 SHA-256 为 `c5edfcaeba5d903b7a588089ec744b1b2f7dd2c668b949f0d8066ceb52b9a066`。

## 发布验证

- Python／Swift 全量合同 `1,442/1,442`、最终定向回归 `170/170`、架构基线 `11/11` 通过。
- fresh `./build.sh`、本地化编译、脚本语法和严格签名检查通过。
- 候选 App 内 60 个 Mach-O 均为 ad-hoc、无 hardened runtime、无证书 Authority 和 Team ID。
- DMG 只读挂载、ZIP 解包、bundle 版本／源码／官方 Radar／Sparkle feed／Ed25519 公钥回读和 `SHA256SUMS` 复验通过。
- ZIP 内冻结后端在隔离 HOME／数据目录中从官方端点刷新到 `snapshot-2026-08-10T06-00-00Z`，取得 8 份快照和最新 15 行榜单。
- 双仓边界、私有远端运行器／发布器 `50/50`、Cloudflare Vitest `37/37`、Workers Runtime `1/1`、Linux／AMD64 Container `/health` 和 Wrangler dry-run 通过。

- annotated tag、GitHub prerelease、四项 GitHub／R2 文件及无认证公开回下载均已核对；两个镜像的字节数、SHA-256 和 `SHA256SUMS` 一致。
- preview appcast 已指向 Build 111 的不可变 R2 ZIP，feed 与 enclosure 的 Ed25519 验签通过；stable appcast 未改动。
- Homebrew Cask 已在隔离目录完成安装、卸载和重装，版本、源码、架构、签名与更新配置均与候选一致，未覆盖 `/Applications/modeldial.app`。
- 官网由 Cloudflare Pages production deployment `d629b5e0-3b94-4104-8c74-5be970c04ba2` 提供；不可变地址与 `modeldial.com` 的双语页面、Radar、数据许可、下载跳转和内置 8 月 10 日官方快照已复验，回滚基线为 `51c42935-aebb-47ec-a74d-8c74421d6e73`。

真实设置页从 Build 110 升级至 Build 111，以及另一台 Gatekeeper／SIP 开启的干净 Apple Silicon Mac 首次安装，仍保留为发布后的人工验收项。

## 已知限制

- 仅支持 macOS 13+ Apple Silicon；Intel Mac 和 Windows 客户端当前不支持。
- 没有 Developer ID、secure timestamp、Apple notarization 或 stapling。
- 本机验证不能替代另一台 Gatekeeper／SIP 开启且无开发环境的 Mac 首次安装验收。
